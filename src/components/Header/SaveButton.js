import React, { useContext } from 'react';
import AppContext from '../../AppContext';
import './style.css';

const SaveButton = () => {

  const button = <button className="save-button">SAVE</button>

  return button;
}


export default SaveButton;

